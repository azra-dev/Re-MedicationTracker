<UserControl x:Class="MedicationTracker.Controls.taskbarUI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MedicationTracker.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="36" d:DesignWidth="255">
    <Grid>
        <StackPanel Grid.Row="0" HorizontalAlignment="Right" Orientation="Horizontal"
            FlowDirection="LeftToRight" Margin="0, 6, 6, 0">
            <Button Style="{DynamicResource taskButton}" Name="taskMin" Click="taskMin_Click">
                <TextBlock Text="-"/>
            </Button>
            <Button Style="{DynamicResource taskButton}" Name="taskMax" Click="taskMax_Click">
                <TextBlock Text="o"/>
            </Button>
            <Button Style="{DynamicResource taskButton}" Name="taskClose" Click="taskClose_Click">
                <TextBlock Text="x" />
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
